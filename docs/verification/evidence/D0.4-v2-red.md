# D0.4 v2 RED Evidence — Exact Environment-Readiness Semantics

- Frozen manifest: `D0.4-v2`
- Frozen manifest path: `docs/verification/manifests/D0.4-v2.yml`
- Frozen manifest SHA-256: `2f5d46d694443fc6d1c91ce594c9a4a43d03a91971773ec80e641a68bccbba0d`
- Previous frozen manifest: `D0.4-v1`
- Previous manifest SHA-256: `de4d61a836e30c35e2c265c76bf48509506b0b797cc1d79b5aaff7c952490959`
- Frozen at: `2026-07-22T12:07:56Z`
- Baseline SHA: `b7eafb683d37d80bd60b59e850311acd79d2a569`
- Probe: `D0.4-V-STATIC-STATIC-CONTRACT`
- Command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-EnvironmentReadiness.ps1 -Mode Static`
- RED completed at: `2026-07-22T12:11:17Z`
- Exit code: `1` (expected RED)
- Oracle: `D0.4 frozen risk/lane manifest fails schema.`

The v2 manifest and the semantic/mutation assertions were frozen before implementation. The first
run failed because the existing risk/lane schema did not admit the new exact
`capabilityContracts` structure. This is the intended structural RED: it proves the verifier was
executed against frozen v2 bytes before the shared schema, readiness inventory, trace generator,
or required-check metadata was changed to satisfy it.

This failure was not caused by authentication, dependency installation, network timing,
permissions, or product code.

## Semantic RED extension

- RED completed at: `2026-07-22T12:14:41Z`
- Exit code: `1` (expected RED)
- Oracle: `D0.4 manifest probes and authoritative trace backlinks differ.`

After the shared schema admitted the frozen contract, all exact capability and mutation probes
passed. The run then failed because the authoritative generated trace contained only the static
D0.4 probe and omitted `D0.4-V-REAL-E2E-HOSTED-WINDOWS`. This is the intended semantic RED for
the generator change: the implementation must add the hosted-Windows target to `PRD:RISK-012`,
regenerate the trace deterministically, and preserve both verification lanes.

## Snapshot-binding RED extension

- RED completed at: `2026-07-22T12:15:46Z`
- Exit code: `1` (expected RED)
- Oracle: `repository snapshot hash drift` was accepted without
  `READINESS-SNAPSHOT-BINDING`.

After the trace generator was corrected, a new mutation replaced the recorded repository-snapshot
digest with 64 zeroes. The verifier accepted the mutation, proving the prior inventory schema alone
did not bind the evidence bytes. The implementation must calculate the normalized snapshot digest
and reject a mismatched path or hash.

## Repository cross-field RED extension

- RED completed at: `2026-07-22T12:16:39Z`
- Exit code: `1` (expected RED)
- Oracle: `repository ruleset-count drift` was accepted without
  `READINESS-REPOSITORY-SNAPSHOT`.

A schema-valid mutation changed the observed ruleset count from zero to one while leaving the
hash-bound snapshot unchanged. The verifier must reject that contradiction as repository-snapshot
drift.

A second cross-field RED completed at `2026-07-22T12:17:02Z`: changing the authenticated
permission from `ADMIN` to `WRITE` was also accepted. That contradiction must produce
`READINESS-REPOSITORY-SNAPSHOT`, because D0.4 closure requires proof that the branch policy can be
configured as well as proof of its observed state.
