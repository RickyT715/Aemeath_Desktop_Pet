# D0.2 v2 RED evidence - runtime discovery artifact acceptance

- Frozen manifest: `docs/verification/manifests/D0.2-v2.yml`
- Frozen manifest normalized-text SHA-256:
  `84bbcd5818b95e452d7bd809c41557cdb86d499ed58d13df20d57854225f3911`
- Frozen at: `2026-08-19T18:34:42Z`
- Baseline source SHA: `0db89c1fbea4152be52e1de9e6f76b214075ead8`
- Branch: `agent/jarvis-tdd-execution`
- RED command started: `2026-08-19T18:43:18Z`
- RED result observed by: `2026-08-19T18:43:42Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Test-CiDelivery.ps1`
- Expected exit: `1`
- Actual exit: `1`

## Frozen test-only state

The required-check input and test harness had the following working-file Git object identities when
RED ran:

| Path | Git object |
|---|---|
| `.github/ci/required-checks.json` | `8e965f594758c9d387c8688952d0d647c89452c0` |
| `tools/ci/Test-CiDelivery.ps1` | `68ab272500080427e0f3815fd12eb9c5c2a5566a` |

The production consumers remained at the baseline objects during RED:

| Path | Baseline Git object |
|---|---|
| `tools/ci/CiDeliveryContract.psm1` | `1a36607db2ee5949e66f59845f7fc65bd57c440e` |
| `tools/ci/Verify-CiWorkflow.ps1` | `a1a31a8fdff1f374e6467c0e1c5e44f295eb81e3` |
| `tools/ci/Wait-ForCi.ps1` | `9ae37adedb989dad6d6b4b1670e352f9c9a51ddd` |

No parser, waiter, workflow verifier, workflow, or producer change existed when this RED was
executed. The valid fixture reached the downloaded-result contract after every inherited static
and result control passed. The failure was not a syntax, setup, dependency, or unrelated legacy
failure.

## Intended oracle and complete mutation census

The pre-v2 artifact consumer accepted all ten independently executed mutations even though each
retained log remained present and the surrounding job/artifact result was otherwise valid:

| ID | Mutated dimension | RED observation |
|---|---|---|
| `D02R-M01` | Remove the required D0.3 marker | accepted |
| `D02R-M02` | Report actual discovery `0` | accepted |
| `D02R-M03` | Report `23`, one below the D0.3 floor | accepted |
| `D02R-M04` | Retain the prefix with malformed JSON | accepted |
| `D02R-M05` | Duplicate the valid D0.3 marker | accepted |
| `D02R-M06` | Add an undeclared result ID | accepted |
| `D02R-M07` | Bind the marker to another 40-hex SHA | accepted |
| `D02R-M08` | Report one skipped probe | accepted |
| `D02R-M09` | Report one failed probe | accepted |
| `D02R-M10` | Encode actual discovery as a JSON string | accepted |

The terminal diagnostic listed the exact complete set:

```text
D0.2R intended RED accepted discovery mutations: D02R-M01, D02R-M02, D02R-M03,
D02R-M04, D02R-M05, D02R-M06, D02R-M07, D02R-M08, D02R-M09, D02R-M10
```

GREEN must reject every row for its stable discovery diagnostic, accept the unmutated eight-marker
fixture, and preserve the existing exact artifact file inventories. A later static RED separately
challenges quoted/no-op command substitution after the central artifact parser is green.

## Claim boundary

This RED demonstrates the accidental/reduced/stub acceptance defect in the baseline central
consumer. The proposed marker remains evidence emitted by independently reviewed exact-SHA source;
it is not cryptographic attestation and does not establish malicious same-commit resistance.

## Review-driven supplemental RED

The first central GREEN candidate passed `D02R-M01..M10`, but independent review produced three
additional concrete counterexamples. Tests were added before repairing that candidate and the same
focused command was run again. By `2026-08-19T18:58:25Z` it exited `1` with exactly these accepted
review mutations:

```text
D0.2R intended RED accepted discovery mutations: D02R-M11, D02R-M12, D02R-M13
```

| ID | Rejected-candidate defect |
|---|---|
| `D02R-M11` | PowerShell 5.1 silently applied last-key-wins to a duplicate JSON property. |
| `D02R-M12` | Parent minimum `999` was accepted although included component minima summed to `24`. |
| `D02R-M13` | Every component could be excluded from aggregation while the public parser still succeeded. |

The test candidate Git object was `437cc38eeb69e6c4728319ab7c20cadd7b7bd257`; the rejected parser
candidate object was `3e6401360e669e76df9fdc5b35248ee8e79ef377`. Review controls
`D02R-M14..M19` already rejected wrong-log, missing-field, extra-field, case-drift, ANSI-prefixed,
and exponent-number inputs, so they remained GREEN during this focused RED. The replacement parser
must close `M11..M13` without weakening any earlier control and must reduce the rejected candidate's
disproportionate implementation size.

## Review-driven Wait-output RED

- UTC time: `2026-08-19T19:09:24.1237469Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Test-CiDelivery.ps1`
- Exit code: `1`

The focused suite completed its inherited static controls and then failed at the Wait discovery
summary oracle for the required reason:

```text
Wait-ForCi.ps1 : A parameter cannot be found that matches parameter name 'SelfTestOnly'.
```

The current working-tree Git blob hashes at this RED point were:

| Path | Git blob hash |
|---|---|
| `tools/ci/Test-CiDelivery.ps1` | `fe6163756dfecb2efeef06871bf8ee83ebbc70c0` |
| `tools/ci/Wait-ForCi.ps1` | `9ae37adedb989dad6d6b4b1670e352f9c9a51ddd` |

## Independent parser review round 1 RED

- Started UTC: `2026-08-19T19:31:38.7113274Z`
- Completed UTC: `2026-08-19T19:31:58.3407174Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Test-CiDelivery.ps1`
- Expected exit: `1`
- Actual exit: `1`

The test-only and pre-fix parser working-file Git objects were:

| Path | Git blob hash |
|---|---|
| `tools/ci/Test-CiDelivery.ps1` | `1be5a289e340853dd1f66bac56040edba9d66165` |
| `tools/ci/CiDeliveryContract.psm1` | `30a97cddad2b2886a19abf523fac06e8337648ad` |

All inherited rows `D02R-M01..M19` rejected their mutations. Both supplemental controls passed
before the intended failure: removing only the excluded `D0.5-V-STATIC-UBUNTU` marker produced the
frozen missing-marker diagnostic, and an included D0.3 actual of `31` was returned and aggregated as
`31` rather than capped to its `24` floor.

The complete accepted census contained only the independent-review counterexamples:

```text
RED accepted discovery mutation: D02R-M20 escaped duplicate key after literal key
RED accepted discovery mutation: D02R-M21 escaped duplicate key before literal key
RED accepted discovery mutation: D02R-M22 physical evidence filename case drift
D0.2R intended RED accepted discovery mutations: D02R-M20, D02R-M21, D02R-M22
```

`M20` and `M21` use semantically duplicate `actualDiscovery` property names with one spelling
encoded as `\u0061ctualDiscovery`, in both orderings. `M22` keeps the frozen logical path but changes
the physical Windows artifact-relative filename from `verification-contract-tests.log` to
`Verification-Contract-Tests.LOG`. No production file changed between this test-only edit and the
RED execution.

## Task 4 executable-command RED

- Started UTC: `2026-08-19T19:47:52.2657280Z`
- Completed UTC: `2026-08-19T19:47:55.0052459Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Test-CiDelivery.ps1`
- Expected exit: `1`
- Actual exit: `1`
- Baseline source SHA: `0db89c1fbea4152be52e1de9e6f76b214075ead8`
- Test-only harness Git blob: `34de748798004ca55fc81d2d1b3362a320aa332d`
- Unchanged verifier Git blob: `a1a31a8fdff1f374e6467c0e1c5e44f295eb81e3`

The valid static contract passed first. The unchanged substring-only verifier then accepted every
independently executed non-executable location while each fixture retained a harmless pipeline into
`verification-contract-tests.log`:

```text
RED accepted static command mutation: D02R-SC01 quoted command
RED accepted static command mutation: D02R-SC02 commented command
RED accepted static command mutation: D02R-SC03 environment-value command
RED accepted static command mutation: D02R-SC04 marker-payload command
RED accepted static command mutation: D02R-SC05 here-string exact pipeline
RED accepted static command mutation: D02R-SC06 block-comment exact pipeline
D0.2R command intended RED accepted mutations: D02R-SC01, D02R-SC02, D02R-SC03,
D02R-SC04, D02R-SC05, D02R-SC06
```

This was the intended oracle: the failure occurred only because quoted output, a comment, an
environment value, a marker payload, a here-string line, and a block-comment line were not rejected
with `DISCOVERY-COMMAND-EXECUTABLE: verification-contracts`. There was no syntax, fixture,
dependency, inherited-control, or workflow-file failure.

## Task 4 discovery-results schema RED

- Started UTC: `2026-08-19T19:51:59.5426773Z`
- Completed UTC: `2026-08-19T19:52:09.5312785Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Test-CiDelivery.ps1`
- Expected exit: `1`
- Actual exit: `1`
- Test-only harness Git blob: `e30e1a67b3d8537c5a8e1334098a775800c263ee`
- Command-GREEN verifier Git blob before schema repair:
  `81553d12ce313a09ef252191024a024c4ccaf6a6`

All six executable-command controls remained GREEN first. The ordinal case-distinct result-ID
positive control also passed. The pre-schema verifier accepted the complete independently executed
census:

```text
D02R-SS01, D02R-SS02, D02R-SS03, D02R-SS04, D02R-SS05, D02R-SS06,
D02R-SS07, D02R-SS08, D02R-SS09, D02R-SS10, D02R-SS11, D02R-SS12,
D02R-SS13, D02R-SS14, D02R-SS15, D02R-SS16, D02R-SS17, D02R-SS18
```

These rows independently covered missing/empty results, missing/extra fields, blank and ordinally
duplicate IDs, safe-but-undeclared/traversal/rooted evidence paths, zero/string/decimal minima,
string/numeric inclusion flags, included-only parent-sum mismatch, and not-applicable results plus
integer/noninteger floor violations. The terminal error was only the complete accepted-mutation
census; fixture construction, Cycle A, and the case-sensitive positive control all succeeded.

### Decimal fixture fidelity supplement

Windows PowerShell 5.1 serializes `[double]24.0` as JSON integer `24`, so the SS12 fixture was
corrected to the stable JSON real value `24.5`. The corrected manifest was independently run against
the baseline verifier blob `a1a31a8fdff1f374e6467c0e1c5e44f295eb81e3` from
`2026-08-19T19:55:20.0733432Z` through `2026-08-19T19:55:20.4769444Z`. The verifier exited `0` and
printed `CI workflow contract passed.`, confirming the intended pre-fix acceptance defect for the
real-number minimum rather than relying on a type-coerced integer fixture.
