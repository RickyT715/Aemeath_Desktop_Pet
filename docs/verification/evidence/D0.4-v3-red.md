# D0.4 v3 RED Evidence — Hosted Runtime Evidence and Discovery

- Frozen manifest: `D0.4-v3`
- Frozen manifest path: `docs/verification/manifests/D0.4-v3.yml`
- Frozen manifest SHA-256: `3747f8520497b65544ef0cd1bb386b8e72866284001da408de111f454f0d614a`
- Previous frozen manifest: `D0.4-v2`
- Previous manifest SHA-256: `2f5d46d694443fc6d1c91ce594c9a4a43d03a91971773ec80e641a68bccbba0d`
- Frozen at: `2026-07-22T12:22:00Z`
- Baseline SHA: `b7eafb683d37d80bd60b59e850311acd79d2a569`
- Probe: `D0.4-V-STATIC-STATIC-CONTRACT`
- Command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-EnvironmentReadiness.ps1 -Mode Static`
- RED completed at: `2026-07-22T12:24:54Z`
- Exit code: `1` (expected RED)
- Oracle: `D0.4 frozen risk/lane manifest fails schema.`

Independent review supplied two reproducible counterexamples after v2 was locally green:

- `CAP-GHA-WINDOWS-CAPABILITY-PROBE` could be changed from `pending` to `ready` while citing only
  the generic repository snapshot, with no exact-SHA hosted runtime artifact; and
- the 16-probe global discovery floor was already satisfied by 22 static probes, so hosted-specific
  OS/UIA/storage/network/cleanup/timeout probes could disappear without failing discovery.

The v3 manifest was frozen before implementation with a precise committed-runtime-evidence
contract and a separate nine-probe hosted discovery floor. The first run failed because the shared
risk/lane schema did not yet admit `hostedRuntimeContract`. This was the intended structural RED;
it was not caused by authentication, dependencies, network timing, permissions, or product code.

## Ready-evidence semantic RED extension

- RED completed at: `2026-07-22T12:26:54Z`
- Exit code: `1` (expected RED)
- Oracle: a schema-valid hosted-ready mutation citing only
  `D0.4-github-snapshot.md` did not produce `READINESS-HOSTED-RUNTIME-EVIDENCE`.

After the v3 structure was admitted, the independent review counterexample was encoded as an
executable mutation. All earlier probes passed, and the mutation was accepted. The implementation
must require the frozen committed runtime path, the green evidence path, a verified source SHA, and
valid runtime bytes whenever the hosted capability is labeled ready.

## Hosted-discovery semantic RED extension

- RED completed at: `2026-07-22T12:27:27Z`
- Exit code: `1` (expected RED)
- Oracle: `Hosted-specific discovery mutation was accepted by the global discovery floor.`

With ready-evidence semantics implemented, a synthetic hosted run reported 100 total probes but
only eight hosted probes. The legacy global-floor calculation accepted it. The implementation must
track hosted passes independently, require all nine frozen hosted checks, and require at least 25
total probes (16 static plus nine hosted) in the hosted CI job.

## DPI-evidence RED extension

- RED completed at: `2026-07-22T12:34:53Z`
- Exit code: `1` (expected RED)
- Oracle: `Generated hosted Windows capability evidence fails its frozen schema.`

Independent UI review found that v3 named display/DPI observation but only persisted screen
dimensions. The runtime schema was changed first to require DPI availability, X/Y DPI, and scale
percentage. After every preceding hosted probe passed, the unchanged runtime writer failed that
schema. The implementation must call the Windows system-DPI boundary and persist either measured
values or an explicit unavailable/zero observation without treating it as an interactive-desktop
pass.

## Exact-command RED extension

- RED completed at: `2026-07-22T12:37:31Z`
- Exit code: `1` (expected RED)
- Oracle: `Delivery-environment required-check and workflow contracts are not aligned.`

The verifier was tightened first to compare required-check metadata with the complete frozen hosted
command. The existing metadata recorded only the script path and therefore failed before the
metadata was changed. The authoritative command must retain `-Mode HostedWindows` and the exact
evidence directory as well as the executable path.

## V1 chronology RED extension

- RED completed at: `2026-07-22T12:38:21Z`
- Exit code: `1` (expected RED)
- Oracle: `D0.4-v1 chronology mutation was accepted by identity-only provenance checks.`

A mutation moved the v1 freeze timestamp to 2099 while retaining the correct manifest identity,
hash, and RED exit code. The preexisting v1 checks accepted it. The provenance verifier must parse
the v1 timestamps, require the exact recorded freeze instant, order freeze before RED, and reject
the chronology mutation just as it does for v2 and v3.

## Independent source/run-binding RED extension

- RED completed at: `2026-07-22T12:39:47Z`
- Exit code: `1` (expected RED)
- Oracle: `Schema-valid wrong-source mutation was accepted without source-identity/run binding.`

The runtime evidence and readiness inventory were assigned the same fabricated `b…b` SHA while a
separately schema-valid retained source-identity record proved `a…a` for push run 123. The old
self-consistency check accepted the runtime/readiness pair. Closure must instead bind the runtime
SHA, readiness SHA, source-identity expected/actual SHA, run ID, committed evidence digests, and
structured Green-document fields to one push execution.
