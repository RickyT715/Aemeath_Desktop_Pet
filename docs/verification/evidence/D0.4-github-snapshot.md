# D0.4 Redacted GitHub Capability Snapshot

- Collected: `2026-07-22T11:52:00Z`
- Repository: `RickyT715/Aemeath_Desktop_Pet`
- Visibility: `PUBLIC`
- Default branch: `main`
- Authenticated repository permission: `ADMIN`
- Collection method: authenticated GitHub CLI REST reads; no secret values were requested or stored

| Capability surface | Redacted result |
|---|---|
| `GET /branches/main/protection` | `404 Branch not protected` |
| `GET /rulesets` | `0` rulesets |
| `GET /actions/runners` | `0` self-hosted runners |
| `GET /actions/secrets` | `0` repository secret names; values are never readable or recorded |
| `GET /environments` | `0` protected environments |
| `gh repo view --json viewerPermission` | `ADMIN`; sufficient to configure the required branch policy after its CI context exists |

The snapshot establishes absence; it does not relabel any required capability optional. Missing
interactive Windows 11 workers and self-hosted capability labels first block P0A.7 when the hosted
runner is not interactive; missing Windows 10 first blocks P0A.8. Missing long-term archive blocks
P0A.9, missing provider
approval blocks H0.1 live-provider evaluation, missing signing blocks P11.1 signed release, and
missing pilot authorization blocks P11.2. GitHub-hosted exact-SHA and 90-day artifact readiness is
separately evidenced by D0.3.

The public repository coordinate, public owner handle, and public Actions run URLs are explicitly
allowed metadata. No token, secret value, local username/profile path, machine identifier,
participant identity, personal document, or private diagnostic payload is present in this snapshot.
