# D0.5 clean-install qualification RED evidence

- Frozen manifest: `docs/verification/manifests/D0.5-v1.yml`
- Frozen manifest SHA-256: `3ba590b540db3838e69ade6a9f298bc139ecc567f9feccae630fd940e103a716`
- Frozen at: `2026-07-22T13:28:31Z`
- Valid RED started at: `2026-07-22T13:34:15Z`
- RED completed at: `2026-07-22T13:34:15Z`
- Command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-DependencyQualification.ps1 -Mode Static`
- Exit code: `1`
- Discovery before failure: `1` static probe

## Intended failure

The independently authored verifier first proved that the risk/lane bytes matched the hash frozen
before execution. It then rejected `D0.5-v1` because the existing shared Draft 2020-12 schema did not
yet admit the new `dependencyQualificationContract`. The required qualification schema and committed
clean-install evidence also did not exist. This is the intended structural RED: implementation must
add the general contract shape, real qualification evidence, trace and CI bindings without changing
the four frozen production manifests.

The observed failure was:

```text
PASS frozen risk/lane bytes match the pre-RED hash
D0.5 risk/lane manifest does not satisfy the shared schema.
```

An earlier launch found and corrected a parser typo in the newly written test harness. That harness
defect is not counted as RED evidence and caused no product, manifest, schema, or oracle change.
The valid run above reached the frozen contract assertion and failed for the intended missing
implementation.
