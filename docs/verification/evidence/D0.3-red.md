# D0.3 RED Evidence — Traceability and Manifest Contracts

- Baseline SHA: `a6c15496cf2893a24e560c01ef4e598a635859cc`
- Probe: `D0.3-V-STATIC-001`
- Date: 2026-07-22
- Command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-VerificationContracts.ps1`
- Exit code: `1` (expected RED)

The probe parsed and executed successfully, then rejected the intentionally absent implementation:

- `docs/verification/traceability-v1.yml`;
- four versioned verification schemas;
- valid risk/lane, evidence, and exact-SHA manifest examples; and
- seven mutation fixtures for missing/duplicate requirements, stale source hashes, invalid
  dispositions, orphan tests, orphan acceptance criteria, and ambiguous bare NFR references.

The failure occurred at the frozen missing-input oracle. It was not a script syntax, dependency,
permission, or environment failure.
