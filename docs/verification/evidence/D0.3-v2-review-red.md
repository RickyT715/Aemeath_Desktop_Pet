# D0.3 v2 Review RED Evidence — Implementable Semantic Mapping

- Frozen manifest: `D0.3-v2`
- Baseline SHA: `a6c15496cf2893a24e560c01ef4e598a635859cc`
- Probe: `D0.3-V-STATIC-001`
- Date: 2026-07-22
- Command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-VerificationContracts.ps1`
- Exit code: `1` (expected RED)

Independent review showed that the structurally complete graph was not yet implementable. New
semantic assertions were added before generator changes. They rejected the unchanged v2
implementation at the intended verification oracle with these failure classes:

- all 11 aggregate current-source locators hashed only a heading, not their represented section;
- the D0.3 catalog declared phantom non-static tests and omitted its real frozen probe;
- six Planned current promises were incorrectly represented as passing Gate 0 cases;
- future acceptance criteria produced one synthetic E2E per criterion rather than shared journeys,
  exceeding the frozen Voice/RAG/proactivity E2E budget;
- representative cross-boundary acceptance, gate, NFR, risk, PB, and UI mappings omitted their
  owning steps or boundaries; and
- schema-valid but contradictory risk, phase-evidence, and exact-SHA objects lacked executable
  cross-field rejection controls.

The run parsed the 352-node artifact and reached the newly authored semantic assertions. It was not
a compilation, dependency, permission, discovery, or environment failure. The v2 scope and five
frozen risks already cover these review-discovered defects, so this is a stricter RED within v2,
not a reduced or retrofitted success definition.
