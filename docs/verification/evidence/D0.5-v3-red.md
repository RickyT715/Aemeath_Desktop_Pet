# D0.5 v3 RED evidence — final independent-review corrections

- Frozen manifest: `docs/verification/manifests/D0.5-v3.yml`
- Frozen manifest SHA-256: `990073c5256b333e6845c54b1376325df6042884d656d42d7cde11563da736f6`
- Previous frozen manifest: `docs/verification/manifests/D0.5-v2.yml`
- Previous manifest SHA-256: `96d9dc5d2029846b0447b2bfb5f09f5e0a30699915138713ec1c6a84249197c4`
- Frozen at: `2026-07-23T06:01:37Z`
- RED completed at: `2026-07-23T06:04:16Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-DependencyQualificationV3Review.ps1`
- Exit code: `1` (expected RED)
- Discovery: four review probes; the immutable freeze probe passed and all three newly frozen
  implementation-gap probes failed.

The v3 contract was required because the final independent v2 review changed observable success
criteria. Exact-wheel qualification now requires installed-distribution PEP 610 `direct_url.json`
archive metadata to bind independently to the downloaded wheel SHA-256 without retaining the local
URL, semantic processing/success validation of both Python install phases, and a mutant that changes
the actual pip argument from the downloaded file to index resolution. Every external git and
fallback schema-validator command must use the bounded runner, timeout cleanup must terminate and
verify every captured descendant on both supported operating-system families, and a real
parent-spawns-child timeout test must prove both PIDs exit. Finally, the Ubuntu static claim must be
executed under `pwsh` in the `Verification Contracts` job and retained as distinct evidence.

The frozen review test produced:

```text
PASS V3-FREEZE v3 and the v2 invalidation are immutable and linked
FAIL V3-WHEEL-RUNTIME runtime PEP 610 provenance, install-log semantics, and actual pip argument mutation are enforced
FAIL V3-PROCESS-TREE git/schema subprocesses are bounded and a real parent-child timeout proves every PID exited
FAIL V3-UBUNTU-TRUTH separate truthful commands exist and CI executes and archives Ubuntu pwsh static evidence
D0.5 v3 review RED (3 blocking gaps)
```

This RED changes no production source, frozen product manifest, older frozen manifest/RED, or legacy
test. It remains qualification evidence for the first two D0.5 checklist items; the delivery item
stays open until a later commit, push, and exact-head-SHA CI gate.

## Delivery-review RED addendum

- RED completed at: `2026-07-23T06:31:13Z`
- Command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-DependencyQualificationV3Review.ps1`
- Exit code: `1` (expected RED)
- Discovery: six independent review probes; four existing probes passed and exactly two newly
  added delivery-review probes failed.

The unchanged implementation did not guarantee that the two CI jobs which execute the bounded
historical-baseline lookup receive enough Git history from `actions/checkout`. It also verified
only the newest D0.5 invalidation fields, rather than schema-validating both invalidation records
and calculating and binding the actual normalized v1 and v2 manifest bytes through the complete
v1-to-v2-to-v3 identity, replacement, hash, and chronology chain.

```text
PASS V3-FREEZE v3 and the v2 invalidation are immutable and linked
PASS V3-WHEEL-RUNTIME runtime PEP 610 provenance, install-log semantics, and actual pip argument mutation are enforced
PASS V3-PROCESS-TREE git/schema subprocesses are bounded and a real parent-child timeout proves every PID exited
PASS V3-UBUNTU-TRUTH separate truthful commands exist and CI executes and archives Ubuntu pwsh static evidence
FAIL V3-HISTORICAL-CHECKOUT both baseline-validating CI jobs fetch history and exact workflow mutations are rejected
FAIL V3-INVALIDATION-CHAIN actual v1/v2 bytes, both schemas, full chronology, and historical-byte mutation are enforced
D0.5 v3 review RED (2 blocking gaps)
```

The frozen v3 manifest remained byte-identical at normalized SHA-256
`990073c5256b333e6845c54b1376325df6042884d656d42d7cde11563da736f6`.

## Exact-SHA CI byte-preservation RED addendum

- Frozen manifest remained unchanged at:
  `990073c5256b333e6845c54b1376325df6042884d656d42d7cde11563da736f6`
- Exact source SHA: `8d6ea68696bfd7818c51bb85ed60cfd9dc6357e5`
- Push run: `29986425811`
- Run URL:
  `https://github.com/RickyT715/Aemeath_Desktop_Pet/actions/runs/29986425811`
- Run chronology: started `2026-07-23T06:52:29Z`; completed `2026-07-23T06:54:41Z`
- Run conclusion: `failure` (required RED)
- Independent regression RED completed at: `2026-07-23T06:58:31Z`
- Independent command:
  `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Test-DependencyEvidenceAttributes.ps1`
- Independent exit code: `1` (expected RED)

The exact-SHA checkout normalized generated dependency-evidence line endings because no Git
attribute declared those files byte-preserved. The authoritative v3 working packet still matched
all seven raw SHA-256 declarations, but both hosted operating systems received filtered LF blobs.
The Ubuntu `Verification Contracts` job `89139289372` therefore failed at:

```text
Evidence hash mismatch for 'tool-versions.json'.
```

The Windows `Dependency Qualification` job `89139289416` failed before the expensive Qualify lane
at:

```text
Evidence hash mismatch for 'dotnet-restore.log'.
```

Both jobs first passed the frozen-manifest, invalidation-chain, command, product-manifest, Schema,
source, exact-file-set, and semantic-coherence checks. This isolates the defect to Git delivery
filters rather than qualification behavior or packet provenance. The new independent test first
proved that missing-rule and `text` mutations are rejected, then failed against the unchanged
implementation:

```text
PASS missing dependency-evidence attribute mutation is rejected
PASS text-normalizing dependency-evidence attribute mutation is rejected
Dependency evidence Git attribute file is missing: .gitattributes
```

Chronology remains strict: v3 freeze (`2026-07-23T06:01:37Z`), independent-review REDs
(`06:04:16Z` and `06:31:13Z`), exact-SHA hosted RED (`06:52:29Z`), then the new local
byte-preservation regression RED (`06:58:31Z`). No authoritative packet content was normalized,
rewritten, or regenerated.
