# D0.2 RED Evidence — Exact-SHA CI Bootstrap

- Baseline SHA: `94903d9c01570aeb600522a9433596e93ceb2bc1`
- Date: 2026-07-22 (Asia/Shanghai)
- Probe: `D0.2-V-STATIC-001`
- Frozen oracle: the pre-D0.2 workflow must fail because it lacks the delivery triggers, exact-SHA checkout and identity assertion, fail-closed required jobs, retained evidence, the required-check manifest, and exact-SHA wait tooling.

## Command

The manifest's portable command is:

```powershell
pwsh -NoProfile -File tools/ci/Verify-CiWorkflow.ps1
```

PowerShell 7 is not installed on the local Windows host, so the equivalent Windows PowerShell command was used:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/ci/Verify-CiWorkflow.ps1
```

## Result

Exit code: `1` (expected RED).

The probe reported these intended contract failures:

- missing `workflow_dispatch` and `agent/**` push triggers;
- missing read-only contents permission and source-SHA selection;
- concurrency did not separate events and exact source SHAs;
- checkout did not explicitly select or verify the source SHA;
- missing `Delivery Contract` and `.NET Release Build` required jobs;
- the workflow did not run its own contract probe;
- artifact retention was below the 90-day contract;
- a required lane used `continue-on-error: true`;
- `.github/ci/required-checks.json` did not exist; and
- `tools/ci/Wait-ForCi.ps1` did not exist.

This is a behavioral contract failure, not a probe syntax, dependency, or environment failure. Two earlier setup attempts were discarded: the local host lacked `pwsh`, and the initial probe revision contained a Windows PowerShell quoting error. The test was corrected and rerun before this evidence was accepted.
