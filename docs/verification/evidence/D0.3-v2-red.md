# D0.3 v2 RED Evidence — Semantic Trace and Executable Schemas

- Superseded manifest: `D0.3-v1` (`invalidated`)
- Frozen manifest: `D0.3-v2`
- Baseline SHA: `a6c15496cf2893a24e560c01ef4e598a635859cc`
- Probe: `D0.3-V-STATIC-001`
- Date: 2026-07-22
- Command: `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verification/Test-VerificationContracts.ps1`
- Exit code: `1` (expected RED)

Independent review invalidated v1 because its count-complete graph still collapsed acceptance leaves
to parent mappings, used positional test IDs, did not hash individual source statements, did not
execute its JSON Schemas, and contradicted CI discovery/artifact requirements.

After freezing v2, the prior validator rejected the upgraded trace at its intended contract boundary:
it could not parse stable child-step/multi-lane test IDs or correlate the expanded catalog. The output
named `TRACE-INVALID-TEST-ID`, lane-correlation failures, and the stricter bare-NFR rule across the
new graph. The script parsed and discovered the 352-node artifact; this was not a compilation,
dependency, permission, or environment failure.

The v2 GREEN implementation must replace those v1 assumptions, execute Draft 2020-12 validation,
verify per-entry locators/hashes and current approval metadata, require the explicit multi-lane
criteria, and retain schema-negative controls.
